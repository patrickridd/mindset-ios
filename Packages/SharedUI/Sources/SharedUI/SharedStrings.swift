import Foundation
import SwiftUI

public enum SharedStrings {
    // Add shared localized strings here
    // MARK: - AI Reflection

    public enum AIReflection {
        public static let title = String(
            localized: "ritual.aiReflection.title",
            bundle: .module,
            comment: "AI reflection card title")
    }
}
